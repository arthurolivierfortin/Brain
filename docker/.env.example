# Brain secrets — copy this file to `docker/.env` and fill in values.
# `docker/.env` is gitignored and must never be committed.

# Required: Google AI Studio key for Gemini Flash extraction (post_turn hook).
# Get one at https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=
