# Checklist — Brain Monitor Phase 3 (polish + alt views)

**Linked spec:** [2026-04-27-brain-monitor-phase3-design.md](2026-04-27-brain-monitor-phase3-design.md)

**Budget:** 1 day. **Roadmap phase:** R:phase-2b.

**Commit emoji convention** (per CLAUDE.md): `🧠 Feature` for [SPEC-1] through [SPEC-9] (new code).

**HTML/JS verification policy** (Phase 3, inherits Phase 1's policy carried through Phase 2): tests for inline JS are deferred to Phase 4 (Next.js + Playwright). All [SPEC-N] in this checklist are HTML/JSX-only and verified by manual eyeballing during the ≥7-day dogfood — `http://localhost:8621/monitor`. Verification anchors are documented per [SPEC-N] below. **ZERO new pytest tests are added in Phase 3** — the entire change set is HTML/JSX inside the single `brain.html` file. The Phase 1 [TEST-1] and Phase 2 [TEST-1] continue to act as the automated coverage and must remain green. This rationale is consistent with the Phase 1 spec's accepted scope ("JS unit tests / browser tests" are out of scope until Phase 4).

**No-regression invariant:** Phase 1's `[TEST-1]` (`test_monitor_route.py::test_monitor_route_serves_html`) AND Phase 2's `[TEST-1]` (`test_graph_route.py::test_graph_route_returns_expected_schema`) MUST continue to pass after Phase 3 lands. The Phase 1 zones (header htop + uptime + version, footer event log, tweaks panel) AND the Phase 2 zones (force-directed graph with click-to-focus, drag-to-pin, pan + zoom, refresh button, activation halos, thought stream with focus filtering, header sparkline) MUST continue to work — verified manually (open `/monitor` after merge, confirm: header bars + sparkline render; footer event log scrolls; tweaks panel toggles; the graph tab still loads and supports all Phase 2 interactions; the thought stream still filters when a graph node is focused). Phase 3 only **adds** components and **modifies** four surfaces from earlier phases: (a) the empty Phase 2 `<div className="left-placeholder" />` is replaced by `<LeftPanel/>`; (b) the Phase 2 center-row JSX previously rendering `<BrainGraph/>` directly is wrapped by `<TabBar/>` + conditional render; (c) the Phase 1 `<Footer>` gains a search input + matches badge; (d) the Phase 1 `useTweaks` is upgraded to read/write localStorage. Phase 1 + Phase 2 [SPEC-N] items are append-only and are **not** edited in Phase 3.

## Code

- [x] [SPEC-1] In `brain.html`, implement `<LeftPanel>` container and wire it into the `<Main>` grid. Replace the Phase 2 `<div className="left-placeholder" />` (column 1 of the middle row) with `<LeftPanel><ClusterMeters stats={stats} /><TopAccessed entries={graph.nodes} focus={focus} setFocus={setFocus} /></LeftPanel>`. Add CSS `.left-panel { display: flex; flex-direction: column; gap: 8px; padding: 8px; min-height: 0; }`. Component is a thin wrapper that takes `children` and renders them in a flex column. **Manual verification anchor:** open `/monitor`; the previously empty 280 px left column is filled with two stacked sections; the page still shows the Phase 2 graph in the middle and the thought stream on the right. — `backend/src/brain/static/brain.html`

- [x] [SPEC-2] In `brain.html`, implement `<ClusterMeters>` component + trend logic. Reads `stats` from props (passed in from `<App>`'s `useStats` hook). Renders the top 8 entries of `Object.entries(stats?.types ?? {})` sorted by count desc as one row each: type label, horizontal `.meter-bar` (inner span width = `(count / total) * 100%`, background = `paletteFor(type)`), count, trend arrow. Trend arrow comes from comparing `count` to `prevRef.current[type]`: emit `↑` (`.trend-up`, cyan) when current > previous, `↓` (`.trend-down`, red) when current < previous, `→` (`.trend-flat`, dim grey) when equal or when `prevRef.current[type]` is undefined. Update `prevRef.current = stats?.types ?? {}` inside a `useEffect(() => {...}, [stats])` so subsequent renders compare against the prior poll. Add the CSS selectors documented in spec §"CSS additions" for `.cluster-meters .*`. **Flicker on `12 → 13 → 12` two-poll oscillation is expected and accepted** — this is dev signal, not user-facing UX (resolves Open Question 1). **Manual verification anchor:** open `/monitor` after seeding entries; left column top section shows up to 8 type rows with bars + counts; trigger a `/store` of a new entry and watch the corresponding row's count increment with a `↑` arrow on the next poll; subsequent stable polls render `→`. — `backend/src/brain/static/brain.html`

- [x] [SPEC-3] In `brain.html`, implement `<TopAccessed>` component + click-to-focus. Reads `entries` (alias for `graph.nodes` from Phase 2's `useGraph`), `focus`, `setFocus` from props. Sorts `entries` by `access_count` desc, slices the top 10, renders a `<ul className="top-accessed">` with one `<li>` per entry. Each row uses CSS grid `8px 1fr auto` and contains: a `<span className="dot" style={{ background: paletteFor(entry.memory_type) }} />`, the truncated content snippet (`entry.content?.slice(0, 40) + (entry.content?.length > 40 ? '…' : '')`), and a `<span className="access-badge">{entry.access_count}</span>`. Click on the `<li>` calls `setFocus(focus === entry.id ? null : entry.id)`. The currently focused row gets the `.focused` class for visual feedback. Add the CSS selectors documented in spec §"CSS additions" for `.top-accessed *`. **Manual verification anchor:** open `/monitor`; left column bottom section shows top-10 most-accessed entries with their type-color dot and access count; clicking a row highlights the corresponding graph node (Phase 2 focus state) and filters the thought stream (Phase 2 behavior); clicking the same row again clears the focus. — `backend/src/brain/static/brain.html`

- [x] [SPEC-4] In `brain.html`, implement `<TabBar>` component + tab state wiring. Component receives `tab` (string: `'graph' | 'matrix' | 'console'`) and `setTab` (function) as props. Renders a `<div className="tab-bar">` with three `<button>` children labeled `graph`, `matrix`, `console`; the button matching `tab` gets the `.active` class. Click on a button calls `setTab(name)`. In `<App>`, derive `setTab = useCallback(name => setTweaks(t => ({ ...t, tab: name })), [setTweaks])` and pass it down. The `tab` value is read from `tweaks.tab` (already declared in Phase 1's `TWEAK_DEFAULTS` with default `'graph'`). Replace the Phase 2 direct `<BrainGraph .../>` render with a new `<div className="center-area"><TabBar tab={tweaks.tab} setTab={setTab}/><div className="tab-content">{tweaks.tab === 'graph' && <BrainGraph .../>}{tweaks.tab === 'matrix' && <MatrixView entries={graph.nodes}/>}{tweaks.tab === 'console' && <ConsoleView events={events}/>}</div></div>`. The Phase 2 `<BrainGraph>` props are passed through unchanged when its tab is active. Add the CSS selectors documented in spec §"CSS additions" for `.tab-bar *` and `.center-area *`. **Manual verification anchor:** open `/monitor`; tab bar shows three buttons above the graph with `graph` active by default; clicking `matrix` swaps the graph for the heatmap; clicking `console` swaps for the JSON tail; clicking `graph` returns to the force-directed simulation; the `<BrainGraph>` rAF loop is properly cancelled when its tab is unmounted (verify by checking DevTools Performance tab — no orphaned animation callbacks after switching away). — `backend/src/brain/static/brain.html`

- [x] [SPEC-5] In `brain.html`, implement `<MatrixView>` SVG heatmap. Reads `entries` from props. Inside a `useMemo([entries])`, compute: `agents = [...new Set(entries.map(e => e.agent || '∅'))]`, `types = [...new Set(entries.map(e => e.memory_type || 'default'))]`, `counts[a][t] = (counts[a][t] || 0) + 1`, `samples[a + '\x00' + t] = [first 5 entry.id values]`, `maxCount = Math.max(...all counts)`. Render an `<svg width="100%" height="100%">` (sized via `ResizeObserver` on the wrapping `<div>`, exact same pattern as Phase 2's `<BrainGraph>` resize) with: column labels at the top (X axis = agents, truncated to 12 chars `label.length > 12 ? label.slice(0, 11) + '…' : label`), row labels on the left (Y axis = types, same truncation), and one `<rect>` per `(agent, type)` cell with `fill={hsla(180, 60%, 50%, alpha(count, maxCount))}` where `alpha(count, maxCount) = count > 0 ? Math.max(0.1, Math.log(count + 1) / Math.log(maxCount + 1)) : 0`. Click on a cell sets local `useState selectedCell = { agent, type, count, sampleIds }`; the overlay is a `<div className="cell-overlay">` positioned absolutely near the cell, listing those values. Click on empty SVG background (i.e., on the `<svg>` element itself, not on a cell `<rect>`) clears `selectedCell`. **No scrolling, no zoom.** Add the CSS selectors documented in spec §"CSS additions" for `.matrix-view *`. **Manual verification anchor:** open `/monitor`, switch to matrix tab; heatmap renders with axes labeled by agent and memory_type; non-zero cells are colored with intensity proportional to `log(count)`; click a cell → overlay appears showing count + first 5 entry ids; click empty space → overlay clears. — `backend/src/brain/static/brain.html`

- [x] [SPEC-6] In `brain.html`, implement `<ConsoleView>` component + `<pre>` + auto-scroll + follow-mode detection. Reads `events` from props (the same Phase 1 `useEvents` 200-event array). Renders a `<div className="console-wrap">` containing a `<pre ref={preRef} onScroll={onScroll}>` of `events.slice(0, 100)` JSON-stringified one per line, each line wrapped in a `<span className={`console-line console-${classifyEvent(e.event_type)}`}>` (where `classifyEvent` returns `green` for `hook_wake_up`, `cyan` for `hook_post_turn`, `yellow` for any event_type containing `reject`, `red` for any containing `error` or `failed`, `grey` otherwise — same palette as Phase 1's `<Footer>` badges). Maintain `followingRef = useRef(true)` and `[showFollowPill, setShowFollowPill] = useState(false)`. Implement `onScroll` to update `followingRef.current = pre.scrollTop + pre.clientHeight >= pre.scrollHeight - 20` and `setShowFollowPill(!followingRef.current)`. In a `useEffect(() => { if (followingRef.current) preRef.current.scrollTop = preRef.current.scrollHeight; }, [events])`, jump to bottom only when in follow mode. Render `<button className="follow-pill" onClick={jumpToBottom}>↓ follow</button>` only when `showFollowPill === true`; clicking it sets scroll to bottom and re-engages follow. Add the CSS selectors documented in spec §"CSS additions" for `.console-wrap *` and `.console-line.*` and `.follow-pill`. **Manual verification anchor:** open `/monitor`, switch to console tab; latest 100 events appear as JSON lines with type-color spans; scrolled to bottom (default), new events keep the view at the bottom (auto-follow); scroll up manually → `↓ follow` pill appears in the top-right and new events stop pushing the view; click the pill → view jumps to bottom and follow re-engages (resolves Open Question 3). — `backend/src/brain/static/brain.html`

- [x] [SPEC-7] In `brain.html`, modify `<Footer>` to add a search input + filter logic + matches badge. Add `useState('')` for `filter` local to `<Footer>`. Add `useRef(null)` for `searchRef` exposed via a `forwardRef` (or via a dedicated `inputRef` prop) so the parent `<App>` can pass its `footerSearchRef` down for the `/` keyboard shortcut. Render a `<div className="footer-search"><input type="search" placeholder="filter…" ref={searchRef} value={filter} onChange={e => setFilter(e.target.value)} /><span className="matches-badge">{filtered.length} / {events.length} matches</span></div>` above the existing Phase 1 events list. Compute `const filtered = filter === '' ? events : events.filter(e => JSON.stringify(e).toLowerCase().includes(filter.toLowerCase()));` and feed `filtered` (instead of `events`) into the existing Phase 1 list rendering. **No debounce.** The Phase 1 list rendering itself is **unchanged** — the only edits are: (1) wrap the existing list with the new search row above, (2) replace the events array iteration variable with `filtered`. Add the CSS selectors documented in spec §"CSS additions" for `.footer-search *`. **Manual verification anchor:** open `/monitor`, type a substring (e.g. `wake_up`) into the footer search input; the events list narrows to matching rows in real time; the matches badge shows `N / 200`; clearing the input restores the full list; the existing Phase 1 type-color badges + relative timestamps continue to render unchanged. — `backend/src/brain/static/brain.html`

- [ ] [SPEC-8] In `brain.html`, implement the keyboard shortcuts handler in `<App>`. Add a single `useEffect` registering a `keydown` listener on `document` and cleaning up on unmount. Inside, ignore the event when `e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA'`. Map keys: `g` → `setTweaks(t => ({ ...t, tab: 'graph' }))`; `m` → matrix; `c` → console; ` ` (space) → `e.preventDefault(); setTweaks(t => ({ ...t, running: !t.running }))`; `r` → `graph.refresh()`; `t` → `setTweaksOpen(o => !o)`; `Escape` → `setFocus(null)`; `/` → `e.preventDefault(); footerSearchRef.current?.focus()`. The `e.preventDefault()` on `/` intentionally suppresses Firefox's quick-find feature — accepted as dev-tool override (resolves Open Question 5). Lift the `<TweaksPanel>` open/closed state into `<App>` as `[tweaksOpen, setTweaksOpen] = useState(false)` (Phase 1 had it local; Phase 3 lifts it for the `t` shortcut) and pass `open={tweaksOpen} setOpen={setTweaksOpen}` as new props to `<TweaksPanel>`. Create `footerSearchRef = useRef(null)` in `<App>` and forward it to `<Footer>` (which assigns it to its `<input>`). Effect dependency array: `[graph.refresh, setTweaks, setTweaksOpen, setFocus]`. **Manual verification anchor:** open `/monitor`; press `g` → tab switches to graph; `m` → matrix; `c` → console; `space` → polling pauses (status dot reflects pause via the existing Phase 1 wiring); `r` → graph refreshes (network tab shows a `/graph` request); `t` → tweaks panel toggles open/closed; `Escape` after focusing a node → focus clears; `/` → footer search input gains focus and the cursor blinks; pressing `g` while typing in the search input does NOT switch tabs (verifies the input-guard). — `backend/src/brain/static/brain.html`

- [x] [SPEC-9] In `brain.html`, upgrade `useTweaks(defaults)` with localStorage persistence. Define `STORAGE_KEY = 'brain-monitor-tweaks-v1'` at module scope. Define a module-scope `let warnedAboutStorage = false` flag. Replace the Phase 1 `useState(defaults)` with `useState(() => readStoredTweaks(defaults))` where `readStoredTweaks` reads `localStorage.getItem(STORAGE_KEY)`, JSON-parses if non-null, returns `{ ...defaults, ...parsed }` so any new field added to `TWEAK_DEFAULTS` since the user's last visit is filled with its default; on any error, log a single `console.warn('[brain-monitor] localStorage unavailable; tweaks will not persist this session')` (gated by `warnedAboutStorage`) and return `defaults`. Replace the raw setter with a `useCallback(updater => setTweaksRaw(prev => { const next = typeof updater === 'function' ? updater(prev) : updater; try { localStorage.setItem(STORAGE_KEY, JSON.stringify(next)); } catch { /* warn-once */ } return next; }), [])`. Phase 1's `setTweaks` callers (accent picker, speed select, pause toggle) and Phase 3's new callers (`tab` switcher, keyboard shortcuts) all flow through this upgraded setter. The Phase 1 component-internal usage of `setTweaks` is **not** edited — the API is preserved (resolves Open Question 4). **Manual verification anchor:** open `/monitor`, change accent color to red, change speed to `2x`, switch tab to `matrix`; reload the page; accent is still red, speed still `2x`, active tab still `matrix`. Open DevTools → Application → localStorage; key `brain-monitor-tweaks-v1` exists with the JSON-stringified state. Open the page in private mode (or block storage in DevTools); the page still works in-memory; the console shows exactly one `[brain-monitor] localStorage unavailable…` warning per session. — `backend/src/brain/static/brain.html`

## Tests

- [ ] [TEST-0] None. Phase 3 is HTML/JSX-only — verified manually during dogfood per the policy in the preamble. Phase 1's `[TEST-1]` (`test_monitor_route.py::test_monitor_route_serves_html`) and Phase 2's `[TEST-1]` (`test_graph_route.py::test_graph_route_returns_expected_schema`) remain unchanged and act as the no-regression coverage; both must remain green under [GATE-2].

[SPEC-1] through [SPEC-9] have no [TEST-N] — they are HTML/JSX-only, verified manually during dogfood per the policy in the preamble. The Phase 1 + Phase 2 precedent applies (Phase 1 had 7 of 8 SPEC items HTML/JSX-only with no test; Phase 2 had 13 of 13 SPEC items HTML/JSX-only with one schema-smoke pytest).

## Storage / Migrations

- [ ] [DB-0] None.

## Verification gates

- [ ] [GATE-1] `cd backend && python -m ruff check .` passes (no Python touched in Phase 3; gate runs on unchanged Python = no-regression check for ruff lint).
- [ ] [GATE-2] `cd backend && python -m pytest -q` passes — Phase 1's `test_monitor_route.py::test_monitor_route_serves_html` AND Phase 2's `test_graph_route.py::test_graph_route_returns_expected_schema` both green. **No new pytest tests added.** This gate is the automated no-regression test for the backend.
- [ ] [GATE-3] frontend gates — (skipped — no frontend package touched). The modified `brain.html` is inert HTML+inline JSX served as a static file; there is still no `frontend/` package, no `pnpm` workspace, no JS test runner. Phase 4 introduces the Next.js + Playwright pipeline.

**Note:** mypy is NOT a hard gate for Brain (99+ pre-existing errors on main, tracked as `docs/improvements/` P2 per CLAUDE.md). Builder may run for advisory signal; failures don't block.
